---
import { Markdown } from "@astropub/md"
import { Badge } from "@/components/ui/badge"
import { normalizeKoreanMarkdown } from "@/lib/markdown/normalize-korean-markdown"
import type { ResumeSummaryBlock } from "@/lib/resume-portfolio/contracts"

interface Props {
  projects: ResumeSummaryBlock[]
}

const { projects } = Astro.props
const summaryMarkdownClass =
  "[&_p]:m-0 [&_code]:rounded-sm [&_code]:border [&_code]:border-resume-border/70 [&_code]:bg-resume-highlight/60 [&_code]:px-1 [&_code]:py-0.5 [&_code]:font-mono [&_code]:text-[0.85em] [&_pre]:mt-2 [&_pre]:overflow-x-auto [&_pre]:rounded-sm [&_pre]:border [&_pre]:border-resume-border/70 [&_pre]:bg-resume-highlight/40 [&_pre]:p-3 [&_pre]:text-xs [&_pre_code]:border-0 [&_pre_code]:bg-transparent [&_pre_code]:p-0"
---

<section
  id="projects"
  class="mb-12"
>
  <h2 class="text-2xl font-bold text-resume-text-main mb-8 tracking-tight">
    Projects
  </h2>
  <div class="space-y-8">
    {
      projects.map((project) => (
        <div
          id={project.resumeItemId}
          class="group bg-resume-card-bg rounded-sm border border-resume-border p-6 flex flex-col transition-all duration-300 hover:border-resume-primary/50 hover:bg-resume-highlight/30"
        >
          <div class="mb-6">
            <h3 class="text-xl font-bold text-resume-text-heading font-heading leading-tight group-hover:text-resume-primary transition-colors mb-4">
              {project.title}
            </h3>

            {project.technologies && project.technologies.length > 0 ? (
              <div class="flex flex-wrap gap-1.5 mb-4">
                {project.technologies.map((tech) => (
                  <Badge
                    variant="outline"
                    className="text-xs font-normal text-resume-text-main bg-resume-bg border-resume-border px-2 py-0.5"
                  >
                    {tech}
                  </Badge>
                ))}
              </div>
            ) : null}

            <div
              data-project-summary
              class:list={[
                "text-resume-text-main/80 mb-6 text-[0.95rem] leading-relaxed",
                summaryMarkdownClass,
              ]}
            >
              <Markdown of={normalizeKoreanMarkdown(project.summary)} />
            </div>

            {project.accomplishments && project.accomplishments.length > 0 ? (
              <ul class="resume-bullet-list space-y-2">
                {project.accomplishments.map((item) => (
                  <li class="pl-4 relative text-[0.95rem] text-resume-text-main/90 leading-relaxed">
                    <span class="absolute left-0 top-2.5 w-1.5 h-1.5 rounded-full bg-resume-primary/70" />
                    <Markdown of={normalizeKoreanMarkdown(item)} />
                  </li>
                ))}
              </ul>
            ) : null}
          </div>

          {project.ctaHref && project.ctaLabel ? (
            <div class="flex justify-start pt-4 mt-auto">
              <a
                href={project.ctaHref}
                class="inline-flex items-center text-sm font-semibold text-resume-primary hover:text-resume-secondary transition-colors focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring rounded-sm group/btn"
                data-ga-event="project_portfolio_click"
                data-ga-project={project.title}
              >
                {project.ctaLabel}
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  width="16"
                  height="16"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  class="ml-1.5 h-4 w-4 transition-transform group-hover/btn:translate-x-1"
                >
                  <path d="M5 12h14" />
                  <path d="m12 5 7 7-7 7" />
                </svg>
              </a>
            </div>
          ) : null}
        </div>
      ))
    }
  </div>
</section>
